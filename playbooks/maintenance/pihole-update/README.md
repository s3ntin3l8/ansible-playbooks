# Pi-hole Update
Updates existing Pi-hole v6 installations on targeted hosts.

## Features
- **Automation**: Runs `pihole -up` and captures the output.
- **Safety**: Verifies the Pi-hole binary exists before attempting an update.
- **Logging**: Displays the update progress in the Ansible output.
