# AI Tools Installation
Installs a suite of AI and LLM development tools on targeted hosts.

## Purpose
This playbook is used to provision existing hosts with AI-specific utilities without re-running the full dev-box configuration.

## Roles Used
- `ai-tools`: Handles the installation of tools like Ollama, Python LLM libraries, and CLI wrappers.
