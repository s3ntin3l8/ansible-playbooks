# Role: homebrew
Installs the Homebrew package manager (Linuxbrew) on Linux hosts.

## Tasks
- **Prerequisites**: Installs dependencies like `build-essential`, `curl`, and `git`.
- **Installation**: Runs the official Homebrew install script non-interactively.
- **Environment**: Automatically configures the Homebrew shell environment in `.bashrc`.
