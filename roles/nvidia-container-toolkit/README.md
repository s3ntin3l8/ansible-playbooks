# Role: nvidia-container-toolkit
Installs the NVIDIA Container Toolkit to enable GPU acceleration in Docker.

## Tasks
- **Repository**: Adds the official NVIDIA package repositories.
- **Installation**: Installs the toolkit and its dependencies.
- **Configuration**: Automatically configures the Docker runtime to use the NVIDIA container runtime.
