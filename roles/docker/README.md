# Role: docker
Installs and configures the Docker engine and Docker Compose.

## Tasks
- **Installation**: Installs Docker and its dependencies.
- **Configuration**: Adds the specified user (`docker_user`) to the `docker` group.
- **Compose**: Ensures Docker Compose is available for orchestrating containers.
