# Role: beszel-agent
Deploys the Beszel resource monitoring agent.

## Modes
- **Docker**: Deploys the agent as a container (preferred for standard hosts).
- **Binary**: Installs the agent as a native systemd service (used for Proxmox hosts).
